# 版本修改履历

所有对外可见的变更记录于此。版本号与 git tag 对应(如 `v1.0`)。

## [1.0] - 2026-09-21

首个正式版本。

### 引擎(core/)

- YAML 驱动执行器:24 个动作(含复合动作 spot_clean),注册表 + priority 分发,新增动作零改 runner
- 视觉定位:SIFT + RANSAC 单应几何校验 + NCC 二次验证,小图标全屏多尺度 NCC 兜底;点击位置日志与红圈标记落盘
- 数据取值:grab/match 与面板动画竞态轮询等待;时间/百分比候选过滤;空间邻近 + 下标回退取值
- 设备前置四项(重启自愈 / 回充等待 20min / 地图加载 6 轮重进 / 电量≥50%),检查结果返回并写入执行结果与报告
- 模板按 APP 前缀管理(`涂鸦_*.png`),换 APP 只改配置
- 12 条用例全部真机通过(MuMu 7555 / SE3L)

### GUI(gui/)

- 用例编排器:卡片式步骤编辑、分类动作菜单、YAML 源码双向同步、深拷贝增删改
- 多用例批量执行:复选框勾选 + 次数轮跑;执行期间锁定编排区,结束无条件恢复(防锁死)
- 用例状态灯:未执行灰 / 执行中黄 / 通过绿 / 失败红,点运行瞬间全部变黄,重新执行自动重置
- 用例排序:行内 ↑↓ 箭头 + 按住拖拽并存,顺序实时写回各用例 YAML 的 `case_order`;CLI 全量执行同序
- 执行结果表:前置检查与每步结果实时落表(列序 #/步骤/结果/耗时/错误,步骤与错误列拉伸、结果列自适应中文),总结行绿/红醒目,长文本悬停看全文
- 截图查看器:小图居中、滚轮缩放(下限=适应窗口)、按住拖拽平移、缩放与滚动条互不干扰
- 设备检测移后台线程;pythonw 完全兼容(无效句柄 WinError 50 修复、无控制台黑框)

### 测试

- 245 passed / 12 skipped(真机安全阀):引擎单测、GUI 离屏测试(真实事件链 QTest)、行为守护测试
- 前置 prepare 返回结果字典;RunWorker 全链路冒烟(mock 设备)

### 修复

- pythonw 下 adb 检测失败 [WinError 50]:子进程 stdin/stderr 句柄显式重定向
- 执行结束后无法编辑/展开步骤:on_run_finished 异常导致 worker 残留 → try/finally 无条件恢复
- set_time 分钟轮 OCR 误读、long_click 秒/毫秒单位、case_list 覆盖覆盖等历史问题见 git 历史
